# Antd-Gin-Admin

通用管理系统脚手架的领域词汇表。当前边界为账号、组织、权限与运维监控类能力；不包含 Web 漏洞扫描或资产台账。

## 领域语言

**用户 (User)**：
系统账号主体；归属某个部门，通过角色获得权限。
_避免使用_: 账号 (Account), 成员 (Member)

**角色 (Role)**：
权限的载体；绑定一组菜单权限，并可配置数据权限范围。
_避免使用_: 权限组 (Permission Group), 用户组 (User Group)

**菜单 (Menu)**：
权限树节点；类型为目录、菜单或按钮，决定可见入口与可执行操作。
_避免使用_: 资源 (Resource), 权限点 (Permission Point)（作节点本体时）

**部门 (Department)**：
树形组织节点；用户归属单位，并与角色的数据权限范围配合做数据隔离。
_避免使用_: 组织 (Organization), 团队 (Team)

### 权限

**菜单权限 (Menu Permission)**：
用户能看到哪些目录/菜单、能执行哪些按钮操作。
_避免使用_: 功能权限 (Feature Permission)

**数据权限 (Data Scope)**：
用户查询数据时的可见范围：全部、本部门、本部门及以下、仅本人。
_避免使用_: 数据隔离 (Data Isolation)（作权限类型名时）

### 运维

**操作日志 (Operation Log)**：
用户关键写操作的审计记录；状态为成功或失败。
_避免使用_: 审计日志 (Audit Log)（作本对象名时）, 系统日志 (System Log)

**邮件告警 (Email Alert)**：
全系统唯一一份的失败操作邮件通知约定（含邮件服务器、发件人、收件人与启用状态）；仅当邮件服务器、发件人、至少一个收件人均已配齐时可以启用。已启用时不可把配置保存成不配齐；未启用时允许保存不完整草稿。仅在已启用时，对之后新产生的失败操作日志立即发送。未启用则不发送、不建告警记录。保存与启用该约定是独立的菜单权限，与查看告警记录、重新发送分开。
_避免使用_: 告警规则 (Alert Rule), 通知策略 (Notification Policy)

**邮件服务器 (Mail Server)**：
邮件告警投递所连接的服务器（地址等）；与发件人、收件人一起构成启用门槛。
_避免使用_: 通道 (Channel)（作本对象名时）

**发件人 (Sender)**：
邮件告警的 From 身份，写在全局约定中；不是产生失败操作日志的用户。
_避免使用_: 用户 (User)（作发件身份时）

**收件人 (Recipient)**：
邮件告警的目标邮箱地址列表（可多个，自由填写）；不是系统用户。一次投递将同一封邮件发给列表中全部地址，发送结果仍落在一条告警记录上。
_避免使用_: 用户 (User)（作收件人时）

**告警记录 (Alert Record)**：
针对某条失败操作日志的唯一邮件告警留痕（一对一）；含操作日志 ID 及发送时的摘要快照（用户、模块、动作、路径、错误信息、发生时间），以及最近一次发送失败原因；状态仅为发送成功或发送失败，无等待态。可见性只受菜单权限约束，不套数据权限。列表是独立的菜单权限。可以删除；删除即永久放弃该次投递留痕，不从操作日志补建。删除是独立的菜单权限。
_避免使用_: 告警日志 (Alert Log), 发送日志 (Send Log), 待发送 (Pending)

**发送失败原因 (Send Failure Reason)**：
最近一次邮件投递未成功的原因摘要；不是操作日志快照里的错误信息。发送成功后清空。
_避免使用_: 错误信息 (Error Msg)（作本字段名时）

**重新发送 (Resend)**：
仅对发送失败的告警记录再次投递邮件；发送成功的不可重新发送。仍更新同一条告警记录，不新建。不依赖邮件告警当前是否启用。重新发送是独立的菜单权限。
_避免使用_: 重试 (Retry)（作自动排队等待时）

**试发 (Test Send)**：
向当前收件人发送一封测试邮件以验证约定是否可用；不对应操作日志，不建告警记录。未配齐时不可试发。属于配置侧菜单权限，不是重新发送。
_避免使用_: 重新发送 (Resend)（作试发时）
