# Antd-Gin-Admin / Web 漏洞扫描

通用管理系统脚手架的领域词汇表；`feature/web-scanner` 分支在此之上增加 Web 漏洞扫描业务。v1 边界为**扫描 + 资产台账**（不做漏洞工单/复测闭环）。下列术语中，RBAC 沿用既有模型；扫描相关术语为本分支新增。

## 领域语言

**用户 (User)**：
系统账号主体；归属某个部门，通过角色获得权限。
_避免使用_: 账号 (Account), 成员 (Member)

**角色 (Role)**：
权限的载体；绑定一组菜单权限，并可配置数据权限范围。
_避免使用_: 权限组 (Permission Group), 用户组 (User Group)

**菜单 (Menu)**：
权限树节点；类型为目录、菜单或按钮，决定可见入口与可执行操作。
_避免使用_: 资源 (Resource), 权限点 (Permission Point)（作节点本体时）

**部门 (Department)**：
树形组织节点；用户归属单位，并与角色的数据权限范围配合做数据隔离。
_避免使用_: 组织 (Organization), 团队 (Team)

### 权限

**菜单权限 (Menu Permission)**：
用户能看到哪些目录/菜单、能执行哪些按钮操作。
_避免使用_: 功能权限 (Feature Permission)

**数据权限 (Data Scope)**：
用户查询数据时的可见范围：全部、本部门、本部门及以下、仅本人。
_避免使用_: 数据隔离 (Data Isolation)（作权限类型名时）

### 扫描与资产

**资产 (Asset)**：
组织长期管理的一条 Web 应用入口台账；粒度为一个规范化根 URL（scheme + host + port + 可选基础路径）。不同子域或不同端口各为一条资产；同一入口下的路径收窄不另建资产，由扫描目标表达。每条资产归属一个部门；列表与操作受角色数据权限约束。同一部门内，未软删资产的规范化根 URL 唯一；跨部门允许重复登记同一 URL。状态为启用 (active) / 停用 (disabled)；停用后不可被新扫描任务引用，历史扫描任务与发现项只读保留。删除为软删，历史引用保留；软删后该部门内可重新登记相同根 URL。v1 不支持跨部门只读共享。
_避免使用_: 资源 (Resource)（与菜单权限树冲突）, 站点 (Site)（作台账实体名时）, 主机 (Host)（作台账实体名时）, 归档 (archived)（v1 不采用）

**扫描目标 (Scan Target)**：
某次扫描任务的输入范围；可引用已有资产，也可是未入库的临时 URL。包含入口 URL（创建时快照，不随资产后续变更而改变），以及用户可填的最大爬取深度与最大页面数；不含路径包含/排除列表。引用资产时可保留资产标识以便回溯，但执行与报告以快照 URL 为准。v1 仅未认证扫描，不包含目标站登录凭证或会话信息。深度与页面数限制为业务字段；策略档位另控制检查强度。临时 URL 不会在创建任务时自动入库；扫描任务成功结束后，可选择将入口保存为资产（默认不保存）。
_避免使用_: 目标 (Target)（无前缀时易与业务目标混淆）, 扫描范围 (Scan Scope)（本产品用「扫描目标」表达该概念）, 扫描凭证 (Scan Credential)（v1 不采用）

**扫描任务 (Scan Job)**：
一次针对单个扫描目标的扫描执行单元；与扫描目标一对一。创建时选择一种扫描策略。状态为：排队 (queued) → 执行中 (running) → 成功 (succeeded) / 失败 (failed) / 已取消 (cancelled)。成功包含「零发现项」。可在排队或执行中取消；取消后已产出的发现项保留。同一资产或同一规范化入口 URL 允许同时存在多条排队/执行中的任务。可见性：扫描目标关联资产时跟随资产所属部门，纯临时 URL 时跟随创建者所属部门。v1 不含定时/周期计划。
_避免使用_: 扫描计划 (Scan Plan), 扫描执行 (Scan Run)（v1 不拆计划与执行）, 扫描 (Scan)（作实体名时过宽）, 部分完成 (partial)（v1 不采用）

**扫描策略 (Scan Policy)**：
扫描任务上的检查强度枚举，非用户可编辑模板。取值为：快速 (quick)、标准 (standard)、深度 (deep)。各档对应的引擎检查集由实现映射，不属于本词汇表。
_避免使用_: 扫描模板 (Scan Template)（v1 不采用）, 扫描配置 (Scan Config)（作实体名时）

**发现项 (Finding)**：
某次扫描任务产出的一条检测结果；归属该任务，不跨任务去重为独立实体。可见性跟随所属扫描任务。引用一条检测规则，并带有严重级别 critical / high / medium / low / info（由引擎写入且 v1 只读；不引入 CVSS 向量）。v1 不引入漏洞台账，也不支持误报/修复等处置状态。
_避免使用_: 漏洞 (Vulnerability)（作 v1 实体名时）, 告警 (Alert), 问题 (Issue)

**检测规则 (Detection Rule)**：
系统中的检查项台账；含稳定规则代码与展示名。由引擎/版本同步写入，控制台不提供自定义增删或改规则逻辑。管理员可将规则启用或停用；停用后新扫描任务不再执行该规则。发现项通过引用检测规则归类。扫描策略到规则子集的映射由系统配置，用户不可编辑该映射。
_避免使用_: 插件 (Plugin)（作业务台账名时）, 规则代码 (Rule Code)（单独作实体名时；代码是检测规则的标识属性）

**扫描报告 (Scan Report)**：
基于某次扫描任务生成的可下载结果汇总；归属该任务。同一任务可多次生成，每次形成一条新的报告版本并保留历史。任务为成功 (succeeded) 或已取消 (cancelled) 时可生成；失败 (failed) 不可生成。v1 不做跨任务的资产级汇总报告。
_避免使用_: 报表 (Statement), 资产报告 (Asset Report)（v1 不采用）
