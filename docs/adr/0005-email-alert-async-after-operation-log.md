# 邮件告警在操作日志落库后异步投递，不阻塞请求、不占用操作日志工作槽

失败写操作需要立即告警，但 SMTP 耗时常以秒计，而操作日志已在请求返回后异步写入，且工作槽饱和时会丢弃日志以保护接口。我们决定：先持久化操作日志并取得 ID，再在独立于该工作槽的路径上发信，最后写入告警记录的最终状态（发送成功或发送失败）；业务请求不等待 SMTP。重新发送与试发由人工触发，可以等待本次 SMTP 结果再返回。未选择「请求内同步发信」或「把发信塞进操作日志中间件同一条异步」，是为了避免拉长失败接口、以及用 SMTP 延迟挤掉审计日志。

**Status:** accepted
